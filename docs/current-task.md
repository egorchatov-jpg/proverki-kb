# Текущая задача / Контекст сессии

> Файл обновляется в конце каждой рабочей сессии с AI.
> AI должен прочитать этот файл в начале работы, чтобы восстановить контекст.

---

## Последнее обновление
- **Дата:** 2026-08-22
- **Сессия:** Бесшовная миграция паролей на scrypt (pkb-v426)
- **Версия кода:** pkb-v426 (последний закоммиченный в master — pkb-v356, 10aa5e8)
- **Статус:** Закоммичено и отправлено в `origin/master`; готово к деплою на Timeweb
- **Коммит:** `4771d12` (`pkb-v420: improve PWA push registration`)

## Что сделано
1. **pkb-v380 — камера в окне фотографий:**
   - Кнопка «Камера» открывает отдельный экран с задней камерой устройства через `getUserMedia`.
   - Если камера недоступна, показывается сообщение «Камера не найдена».
   - После снимка открывается предпросмотр с кнопками «Отмена» и «Применить».
   - «Отмена» возвращает в камеру, выход из камеры возвращает в окно фотографий.
   - «Применить» сохраняет снимок тем же путем, что и «Прикрепить».
   - `APP_BUILD` / SW cache → `pkb-v380`.
2. **pkb-v382 — camera/photo UX:**
   - Камера использует заднюю камеру через `facingMode: environment`; при отсутствии rear camera показывает «Камера не найдена».
   - Кнопка переименована в «Фото», добавлены доступные camera zoom levels (`x1`, `x2`, `x5`) и tap-to-focus.
   - Предпросмотр и существующий просмотр фото поддерживают pinch zoom, pan и double-tap zoom до 2x.
   - Доступ к камере сохраняется браузером/PWA после первого разрешения через localStorage marker; повторный вход не инициирует новый запрос разрешения.
   - iOS file attach оставлен без `capture`, поэтому открывает медиатеку.
   - Long-press превью блокирует выделение текста и системное контекстное меню.
   - Версия приложения `1.08`; `APP_BUILD` / SW cache → `pkb-v382`.
3. **pkb-v383 — права на фото:**
   - Просмотр фото доступен всем пользователям.
   - Кнопки «Камера» и «Прикрепить», а также выделение и удаление фото доступны только суперпользователю и администратору.
   - `APP_BUILD` / SW cache → `pkb-v383`.
4. **pkb-v384 — photo UI:**
   - В просмотре фото сохранённой проверки заголовок изменён на «Фотографии нарушений».
   - Для проверки без фото кнопка в подробном окне называется «Нет фото»; для обычных пользователей она тусклая и недоступная, администратор и суперпользователь могут открыть раздел для добавления.
   - В Windows viewer поддерживает zoom колесом мыши и перемещение фото зажатой левой кнопкой.
5. **pkb-v385 — photo button and desktop drag:**
   - Для проверки с работоспособным барьером кнопка в подробном окне показывает «Нет фото».
   - После добавления первого фото кнопка обновляется на «Фото».
   - Прочерки в полях «Барьер работоспособен» и «Описание нарушений» используют одинаковый серый placeholder-цвет.
   - Перемещение фото мышью в Windows доступно также при масштабе `1x`.
6. **pkb-v386 — photo button after deletion:**
   - После удаления последнего фото в текущем окне кнопка сразу меняется с «Фото» на «Нет фото».
7. **pkb-v387 — placeholder opacity:**
   - Затемнение заблокированных ячеек перенесено с контейнера на дочернее содержимое, чтобы прочерки в «Барьер работоспособен» и «Описание нарушений» имели одинаковый серый цвет.
8. **pkb-v388 — exact placeholder color:**
   - Для прочерков `val-works` и `val-desc` задан одинаковый принудительный цвет `#aaa` и отключено дополнительное opacity самого placeholder.
9. **pkb-v389 — защита фото новых проверок:**
   - Генерация нового `checkId` учитывает не только записи `records`, но и оставшиеся строки `photos`, чтобы удалённые проверки не переиспользовали ID с чужими фото.
   - Старые фото `086126_1–3` удалены; актуальным остался только `086126_4_21.08.2026.avif`.
10. **pkb-v390 — права фото по контексту:**
   - В новой проверке добавление фото доступно всем пользователям с активной сессией.
   - В уже внесенной проверке добавление и удаление фото доступны только администратору и суперпользователю.
11. **pkb-v391 — камера и просмотр фото:**
   - Добавлен pinch zoom камеры и сохранён tap-to-focus.
   - Double-tap viewer увеличивает фото с учётом точки касания.
   - Touch-события viewer не блокируют кнопку закрытия-крестик.
12. **pkb-v392 — camera/photo fixes:**
   - Оранжевые кнопки «Фото» и «Применить» получили повышенную CSS-специфичность.
   - Tap-focus камеры дополнен обработкой touchend.
   - Перемещение фото touch включено независимо от масштаба.
   - Ограничение iOS: стандартный web file picker нельзя принудительно ограничить только медиатекой средствами веб-PWA.
13. **pkb-v393 — camera permission gate:**
   - Добавлена проверка permission state и localStorage marker, чтобы после первого разрешения приложение не инициировало повторный запрос.
   - Если браузер сбросил разрешение после первого предоставления, показывается инструкция по разрешению в настройках вместо повторного системного запроса.
14. **pkb-v394 — platform-specific photo controls:**
   - На iOS и Windows кнопка «Камера» скрыта.
   - «Прикрепить» на iOS и Windows центрируется; Android сохраняет две кнопки «Камера» и «Прикрепить».
15. **pkb-v395 — broken photo cleanup:**
   - `/api/photos/list/:checkId` больше не возвращает строки БД, если физический AVIF-файл отсутствует; такие строки удаляются автоматически.
   - Загрузка фото удаляет AVIF-файл при ошибке вставки строки в БД, чтобы не создавать неполную пару файл/запись.
   - Production на момент проверки отдавал `404` для `/api/photos/export`, что означает необходимость деплоя актуального коммита с ZIP endpoint.
16. **pkb-v396 — iOS/Windows photo button:**
   - Только на iOS и Windows кнопка «Прикрепить» называется «Фото».
   - Её ширина равна нижней кнопке «Назад»: `calc((100% - 20px) / 3)`.
   - На Android сохраняется название «Прикрепить» и текущая flex-ширина.
17. **pkb-v397 — photo screen description:**
   - Для сохраненной проверки без чек-листа под заголовком окна фотографий показывается текст из ячейки «Описание нарушений».
18. **pkb-v398 — актуальный источник фото в viewer:**
   - Viewer открывает фото по имени текущего файла, а не по случайному `img.src` fallback-ветки `<picture>`.
   - JPEG fallback получает cache-busting query, чтобы старое удалённое изображение не подменяло новое.
19. **pkb-v399 — photo button flicker:**
   - Кнопка фото в подробном окне скрыта до завершения проверки наличия фото и сразу показывается в правильном состоянии без видимого переименования/затемнения.
20. **pkb-v400 — local photo viewer:**
   - Viewer корректно открывает локальные `data:image/*` превью и не преобразует data URL в серверный путь.
21. **pkb-v402 — photo button placement:**
   - Кнопка «Добавить фото» перенесена в отдельную строку сразу под «Барьер работоспособен».
   - В новой проверке кнопка показывается только при значении «Нет» и сразу активна.
   - Блок «Описание нарушений» больше не занимает место кнопки.
22. **pkb-v403 — photo button label:**
   - В новой проверке после добавления первого фото кнопка сразу меняет «Добавить фото» на «Фото».
23. **pkb-v404 — photo view access:**
   - Кнопка «Фото» в подробном окне назначает обработчик всем авторизованным пользователям, имеющим право просмотра.
   - Право добавления и удаления фото по-прежнему проверяется отдельно и доступно только admin/superuser для сохранённых проверок.
24. **pkb-v405 — photo edit workflow:**
   - Кнопка фото в подробном окне расположена сразу под «Барьер работоспособен».
   - Для проверки без фото кнопка скрыта для обычных пользователей и доступна только admin/superuser.
   - Для admin/superuser добавленные/удалённые фото в сохранённой проверке проходят staging до «Готово»; «Отмена» отбрасывает изменения.
25. **pkb-v406 — photo navigation permissions:**
   - Обычные пользователи возвращаются из «Фотографий нарушений» сразу в подробное окно проверки.
   - Admin/superuser возвращаются в окно изменения проверки для подтверждения фото через «Готово» или «Отмена».
26. **pkb-v407 — rollback photo edits:**
   - При «Отмена» в карточке изменения восстанавливается исходный список локальных фото, добавленные фото больше не остаются в подробном окне.
27. **pkb-v408 — no-change photo navigation:**
   - Если admin/superuser только просмотрел фото и не изменил список, кнопка «Назад» возвращает сразу в подробное окно.
   - В карточку изменения пользователь попадает только при наличии добавлений или удалений для сохранения.
28. **pkb-v409 — detail photo button flicker:**
   - Фото-кнопка создаётся невидимой inline-стилем и становится видимой только после определения наличия фото и прав пользователя.
29. **pkb-v410 — photo preview deduplication:**
   - Клиент не добавляет повторно превью с тем же серверным filename или локальным tempId при повторном ответе API/рендере.
30. **pkb-v411 — photo preview refresh:**
   - При повторном открытии экрана фото контейнер превью очищается перед рендером.
   - В сохранённой проверке после получения серверного списка временные локальные превью не добавляются поверх актуальных серверных фото.
31. **pkb-v412 — checklist description update:**
   - При применении изменений чек-листа `cardData.desc` и ячейка описания обновляются синхронно до запуска серверной синхронизации.
32. **pkb-v413 — hide photo button for working barriers:**
   - В подробном окне и карточке изменения кнопка фото полностью скрыта при `Барьер работоспособен = Да` для всех ролей.
33. **pkb-v414 — edit photo button label:**
   - В карточке изменения при значении `Нет` кнопка называется «Добавить фото».
34. **pkb-v415 — unified add-photo label:**
   - Все динамические состояния без фотографий используют название «Добавить фото» вместо «Нет фото».
35. **pkb-v416 — photo upload race fix:**
   - «Готово» в карточке изменения ожидает завершения загрузки и удаления фото перед возвратом в подробное окно.
   - Это исключает одновременное отображение временного локального и серверного превью одного снимка.
36. **pkb-v417 — offline photo queue:**
   - Фото сохраняются в IndexedDB до успешного ответа сервера.
   - Очередь повторяется при появлении сети/возврате PWA на экран.
   - Фото удаляется из очереди только после успешной загрузки.
   - Upload completion с ошибками больше не очищает временные фото.
   - Фото на сервере получают имя `{checkId}_{номер}_{дд.мм.гггг}.avif`.
   - В запрос передаётся `clientPhotoId`; повторная отправка одного фото возвращает существующий файл без создания дубля.
   - В IndexedDB хранится очередь `pkb-photo-queue` со store `uploads`; повтор выполняется при появлении сети и возврате PWA на экран.
37. **pkb-v418 — update overlay deduplication:**
   - Показ overlay обновления блокируется в `sessionStorage` по ключу конкретной версии (`build:<serverBuild>` или `waiting:<APP_BUILD>`).
   - Повторные вызовы lifecycle/update checks в одной сессии больше не показывают overlay дважды подряд.
38. **pkb-v419 — Android native camera:**
   - На Android кнопка «Камера» открывает системную камеру через `input[type=file][capture=environment]`.
   - iOS/Windows поведение кнопок не изменено.
39. **pkb-v420 — PWA push registration:**
   - Подписка на push теперь создаётся после `navigator.serviceWorker.ready`, а не сразу после `register`, чтобы iOS/Android не теряли подписку до активации SW.
   - Проверка permission дополнительно учитывает отсутствие `Notification API`.
   - После деплоя необходимо проверить `/api/version`: production должен вернуть `build: "pkb-v420"`.
40. **pkb-v421 — file button label:**
   - На Windows и Android кнопка со скрепкой называется «Файлы».
   - На iOS сохраняется подпись «Фото».
41. **pkb-v422 — organization password sync:**
   - Перед проверкой PIN новой проверки приложение дожидается загрузки актуальных серверных настроек.
   - Это предотвращает проверку нового пароля организации по устаревшему локальному кэшу на новом устройстве.
42. **pkb-v423 — PIN settings refresh:**
   - Перед каждым показом PIN для неавторизованного пользователя выполняется принудительный запрос `/api/settings` с `cache: no-store`.
   - Только после завершения запроса открывается окно ввода пароля.
   - Production-домен: `proverkikb.tw1.ru`.
43. **pkb-v424 — compatible server auth/session:**
   - Добавлен `/api/auth`: сервер проверяет текущие legacy-пароли без их сброса и создаёт HttpOnly/Secure/SameSite session cookie.
   - Существующий offline PIN fallback сохранён: при отсутствии сети текущие пользователи продолжают работать локально и отправлять очереди после восстановления сети.
   - Клиентский PIN сначала проверяется сервером, затем применяет роль/организацию; при offline использует прежнюю локальную проверку.
   - Это первый миграционный этап; перед полным закрытием API нужно отдельно мигрировать пароли на Argon2id и добавить серверные role checks ко всем mutating endpoints.
44. **pkb-v425 — server session write protection:**
   - Session cookie из `/api/auth` используется для мутирующих API сохранения, изменения, удаления, настроек и фото.
   - Legacy PIN fallback сохраняется при offline; queued операции не теряются при истечении/отсутствии серверной сессии.
45. **pkb-v426 — seamless password hash migration:**
   - При первом успешном онлайн-входе legacy-пароль дополнительно мигрируется в `scrypt`-хэш без изменения самого пароля.
   - Сервер сначала проверяет хэш, затем временно legacy plaintext для миграции старых пользователей.
   - Hashes сохраняются при клиентских обновлениях настроек и не требуют сброса паролей.
   - Переменная `AUTH_ENFORCE=1` включает строгую серверную проверку сессий; по умолчанию legacy API остаётся совместимым для старых offline-клиентов.
   - Сервер принимает `clientPhotoId` и повторную отправку того же фото возвращает как duplicate, без создания второй строки/файла.
1. **pkb-v378 — растягивание кнопок без выхода за экран приложения:**
   - Кнопки годов, паспортов, настроек и резервных копий растягиваются по ширине родительского контейнера приложения, а не по `100vw` окна браузера Windows.
   - Убраны Windows-переопределения с шириной через `100vw`.
   - Для вертикальных списков использован `flex: 0 1 auto`, чтобы кнопки не растягивались по высоте; высота кнопок сохранена.
   - После обнаружения синтаксической ошибки CSS внутри JavaScript код возвращён к `pkb-v378`, CSS перенесён в секцию стилей, работа лобби и Windows-стили восстановлены.
   - `APP_BUILD` / SW cache → `pkb-v378`.
1. **pkb-v372 — фото и резервные копии:**
   - Ресайз новых фото изменён с 640px до 1080px по большей стороне.
   - Новые фото сохраняются только в AVIF; постоянные WebP/JPEG-копии не создаются.
   - Просмотр серверных фото использует fallback AVIF → динамический WebP → динамический JPEG через `<picture>`.
   - Старые одноимённые WebP/JPEG fallback-файлы удаляются при обработке соответствующего фото.
   - Резервная копия теперь содержит `proverki.db` и папку `photos/` внутри snapshot.
   - Лимит резервных копий уменьшен с 10 до 5.
   - Восстановление новых backup восстанавливает базу и фотографии согласованно; старые SQLite-only backup остаются совместимыми.
   - GitHub-синхронизация backup дополнена загрузкой и восстановлением файлов фото.
   - `APP_BUILD` / SW cache → `pkb-v373`.
2. **pkb-v375 — каскадное удаление фото:**
   - Удаление проверки теперь удаляет связанные строки из таблицы `photos` и файлы AVIF/fallback с диска.
   - Это предотвращает появление фото удалённых проверок при повторном использовании `checkId`.
   - Для локальной проверки `086026` удалены старые фото 1–3; осталось актуальное `086026_4_21.08.2026.avif`.
3. **pkb-v377 — номера фото и восстановление backup:**
   - Номер серверного фото в приложении теперь берётся из имени файла (`..._4_...`), поэтому совпадает с номером в ZIP.
   - После успешной загрузки локальное превью получает серверное имя и тот же номер.
   - При восстановлении backup папка текущих фото всегда очищается; затем восстанавливаются только фото выбранной копии.
1. **pkb-v361 — иконка несинхронизированной проверки:**
   - В списке «Проверки» используется SVG из `arrows_sort_icon_151591.svg` (`arrows-sort`), красный цвет `#c0392b`.
   - Для иконки задан фиксированный контейнер `18x18px`; строки списка и ячейки имеют фиксированные размеры, чтобы высота строки не менялась.
   - Service Worker cache обновлён до `pkb-static-v361` / `pkb-api-v361`.
   - Для демонстрации добавлен URL-параметр `?demo-unsynced=1`: последняя запись списка отображается без `checkId` только локально, серверные данные не меняются.
2. **Фото в сохранённых проверках:**
   - Кнопка «Добавить фото» добавлена в подробное окно проверки и расположена ниже «Заполнить чек-лист» / «Чек-лист».
   - Для открытых сохранённых проверок передаются `checkId`, чек-лист и локальные фото; серверные фото загружаются через `/api/photos/list/:checkId`.
   - Фото, добавленные к синхронизированной проверке, загружаются сразу; фото офлайн-записи сохраняются локально и отображаются повторно.
   - Возврат из экрана фото ведёт обратно в подробное окно сохранённой проверки.
3. **Оверлей после внесения проверки:**
   - Кнопки «Выйти» и «Следующая проверка» скрыты во время синхронизации и показываются после любого результата: успех, ошибка сервера, отсутствие сети или таймаут.
   - Ширина кнопок оверлея приведена к ширине «Отмена» / «Готово» в карточке проверки.
1. **pkb-v357 — единая ширина кнопок во всех оверлеях:**
   - `.overlay-btn` (базовый класс для всех кнопок оверлеев): добавлена фиксированная ширина `calc((100% - 20px) / 3)` и `flex: none` — теперь обе кнопки («Отмена» и «Применить») имеют одинаковый размер.
   - `.overlay-btns`: добавлено `justify-content: center` — кнопки выровнены по центру.
   - Убраны специфичные переопределения ширины у `#overlay-push-prompt` и `#overlay-app-update` — теперь они тоже используют единую ширину.
   - `APP_BUILD` → `pkb-v357`, кэш SW → `pkb-static/api-v357`.
2. **pkb-v356 — исправление загрузки фото:**
   - **Причина ошибки «Not found»:** в `server.js` `/api/photos` монтировался через `app.all('/api/photos', ...)` — Express не передавал подпути (`/upload`, `/list`, `/delete`) в обработчик. Запросы на `/api/photos/upload` возвращали 404.
   - **Исправление `server.js`:** `/api/photos` убран из `API_ROUTES`, добавлен отдельный `app.use('/api/photos', photosHandler)` — теперь подпути корректно передаются в обработчик.
   - **Исправление `api/photos.js`:** регулярки изменены с `^\/api\/photos\/...` на `^\/...` (относительные пути), так как `app.use` убирает mount-префикс из `req.url`.
   - Проверено локально: тестовая загрузка фото возвращает `200 OK` и сохраняет файл.
   - `APP_BUILD` → `pkb-v356`, кэш SW → `pkb-static/api-v356`.
2. **pkb-v355 — единая ширина кнопок в оверлее успеха:**
   - `.success-btn-next` («Следующая проверка»): добавлена фиксированная ширина `calc((100% - 20px) / 3)` и `flex: none` — теперь совпадает с кнопкой «Выйти».
   - `.success-btns`: добавлено `justify-content: center` — обе кнопки выровнены по центру оверлея.
   - `APP_BUILD` → `pkb-v355`, кэш SW → `pkb-static/api-v355`.
2. **pkb-v354 — удаление дублей в паспорте ДТП и проверка всех паспортов:**
   - **Найдены и удалены дубли в `passports/dtp-01.json`:**
     - `ДТП.08.3.2. Сигнальщик не находится на траектории движения ТС/ спецтехники` — удалены 2 лишних дубля (было 3, остался 1).
     - `ДТП.06.2.1. Каждый факт превышения водителем скорости...` — удалён 1 лишний дубль (было 2, остался 1).
   - **Проверены все паспорта (`rv-01`, `elb-01`, `gruz-01`, `gaz-01`, `dtp-01`) на дубли вопросов чек-листов** — скриптом `node` проверен каждый JSON-паспорт, дубли найдены только в `dtp-01.json`.
   - `APP_BUILD` → `pkb-v354`, кэш SW → `pkb-static/api-v354`.
2. **pkb-v353 — локальное хранение фото до сохранения проверки:**
   - **Клиент:**
     - Фото прикрепляются до сохранения проверки — хранятся локально в `cardData._localPhotos` как `{ base64, violationIndex, tempId }`.
     - При открытии `screen-card-photo` показываются и локальные (`data:image/jpeg;base64`), и серверные (`/photos/`) фото.
     - При быстром тапе локальное фото открывается для просмотра через base64.
     - Удаление локального фото — мгновенно из `cardData._localPhotos` и DOM, без серверного запроса.
     - `cancelCard()` / `resetCard()` — очищают `cardData._localPhotos` автоматически (фото теряются при отмене).
   - **Сохранение:**
     - `submitCard()` — после получения `checkId` от сервера (в callback `syncToServer`) локальные фото загружаются на сервер через `uploadLocalPhotos()`.
     - `submitCardEdit()` — при редактировании существующей проверки локальные фото загружаются сразу (так как `checkId` уже есть).
     - После успешной загрузки `cardData._localPhotos` очищается.
     - `_localPhotos` исключается из `toSync` перед добавлением в очередь синхронизации (не попадает в localStorage).
   - `APP_BUILD` → `pkb-v353`, кэш SW → `pkb-static/api-v353`.
2. **pkb-v352 — единая ширина и центрирование кнопок в карточке проверки:**
   - `.card-btn-submit` («Готово»): добавлена фиксированная ширина `calc((100% - 20px) / 3)` и `flex: none` — теперь совпадает с кнопкой «Отмена».
   - `.card-bottom`: добавлено `justify-content: center` — обе кнопки выровнены по центру экрана.
   - `APP_BUILD` → `pkb-v352`, кэш SW → `pkb-static/api-v352`.
2. **pkb-v351 — загрузка фотографий нарушений:**
   - **Сервер:**
     - Новый API `api/photos.js`: endpoints `/api/photos/upload` (POST), `/api/photos/list/:checkId` (GET), `/api/photos/delete/:checkId/:filename` (DELETE).
     - Используется `sharp` для ресайза (max 640px по любой стороне, конвертация в JPEG, quality 85).
     - Файлы сохраняются в папку `photos/` рядом с базой данных (`data/photos` или `/tmp/proverki-kb/photos`).
     - Имя файла: `{checkId}_{index}_{дд.мм.гггг}.jpeg`.
     - Таблица `photos` в SQLite: `id`, `check_id`, `filename`, `violation_index`, `uploaded_at`.
     - `sharp` перенесён из `devDependencies` в `dependencies`.
     - Static serve для `/photos/*` добавлен в `server.js`.
   - **Клиент (`screen-card-photo`):**
     - Кнопка "Прикрепить" открывает `<input type="file" accept="image/*">`.
     - Фото конвертируется в base64 и отправляется на сервер.
     - Загруженные фото отображаются как миниатюры (ширина 1/4 экрана, `aspect-ratio: 1`, центрирование).
     - Миниатюры группируются над той кнопкой "Прикрепить", с которой были загружены (по `violation_index`).
     - Под каждой миниатюрой — порядковый номер фото.
     - **Long-tap** (500ms) — выделение миниатюры + появление красного круглого крестика в правом верхнем углу для удаления.
     - **Быстрый тап** — открытие полноэкранного просмотра (`overlay-photo-viewer`).
     - Фото можно удалить только после подтверждения (`confirm`).
   - `APP_BUILD` → `pkb-v351`, кэш SW → `pkb-static/api-v351`.
2. **pkb-v350 — ширина относительно видимого экрана + закругление фото-кнопок:**
   - **Ширина всех нижних кнопок (`Назад`/`Отмена`/`Выход`):**
     - `calc((100vw - 48px) / 3)` заменён на `calc((100% - 20px) / 3)` — теперь ширина отсчитывается от родительского контейнера (видимый экран приложения), а не от всего окна браузера.
     - На Windows (`is-win`) приложение центрировано (`left: 28.333%; right: 28.333%`), поэтому `100%` родителя = ширина центрального блока (~43vw), и кнопка занимает ровно 1/3 видимой области.
     - На мобильных поведение не изменилось: `(100% - 20px) / 3` ≈ `(100vw - 48px) / 3`.
     - Затронутые классы: `.screen-back-btn`, `.find-bottom-btn`, `.detail-back-btn`, `.card-btn-cancel`, `.corr-btn-cancel`, `.overlay-btn-cancel`, `.success-btn-exit`.
   - **Кнопки "Камера" и "Прикрепить" (`screen-card-photo`):**
     - `border-radius: 10px` → `border-radius: var(--btn-radius)` — теперь закругление совпадает со всеми остальными кнопками в приложении.
   - `APP_BUILD` → `pkb-v350`, кэш SW → `pkb-static/api-v350`.
2. **pkb-v349 — единая ширина нижних серых кнопок (без изменения высоты):**
   - **Исправление v348:**
     - `.menu-btn-cancel` (install-device, bpk-choice) и `.pin-back-btn` (PIN-экран) — **возвращены как были**: `padding: 13px 40px`, без фиксированной ширины. Высота кнопок ввода паролей восстановлена.
     - Остальные кнопки получили только ширину `calc((100vw - 48px) / 3)` без изменения высоты:
       - `.find-bottom-btn` (find, corrective-list, sokb-list) — `flex: 1` → фиксированная ширина, `height: 53px` сохранён.
       - `.detail-back-btn` (подробное окно) — `padding: 0 40px` → `width: ...; padding: 0`, `height: 53px` сохранён.
       - `.card-btn-cancel` (card, card-checklist) — добавлена ширина, высота из `.card-btn` = 53px сохранена.
       - `.corr-btn-cancel` (corrective-edit, sokb-edit) — `flex: 1` → фиксированная ширина, `height: 53px` сохранён.
       - `.overlay-btn-cancel` (все оверлеи) — добавлена ширина, высота из `.overlay-btn` = 53px сохранена.
       - `.success-btn-exit` (success-overlay) — добавлена ширина, высота из `.success-btn` = 53px сохранена.
       - `.screen-back-btn` — базовая ширина `calc((100vw - 48px) / 3)` без изменений, удалены переопределения v347.
   - **Лобби (`screen-proverki-menu`):**
     - Кнопка "Назад" вынесена в `.settings-footer` с классом `.screen-back-btn`.
   - `APP_BUILD` → `pkb-v349`, кэш SW → `pkb-static/api-v349`.
2. **pkb-v347 — унификация высоты и ширины кнопок:**
   - **Высота кнопок "Камера" и "Прикрепить" (`screen-card-photo`):**
     - CSS `.photo-action-btn`: добавлен `height: 53px`, вертикальный padding убран (`padding: 0 12px`) — высота совпадает с кнопкой "Назад" в этом же окне.
   - **Ширина кнопок "Назад" в `screen-find`, `screen-meropr-menu`, `screen-card-photo`:**
     - `#screen-find .find-btn-cancel`: убрана фиксированная ширина, добавлен `padding: 0 40px` — ширина совпадает с `.detail-back-btn` (подробное окно проверки).
     - `#screen-meropr-menu .screen-back-btn` и `#screen-card-photo .screen-back-btn`: `width: auto; padding: 0 40px` — аналогично кнопке "Назад" в детальном окне.
   - `APP_BUILD` → `pkb-v347`, кэш SW → `pkb-static/api-v347`.
2. **pkb-v346 — UI/UX доработки (синхронизация, карточки, оверлеи):**
   - **Лобби — только синхронизированные нарушения:**
     - `renderLobby`: фильтр `&& r.checkId` — в баннерах нарушений отображаются только записи с присвоенным номером проверки.
   - **Список проверок — индикатор несинхронизированной записи:**
     - `renderFindList`: для записей без `checkId` отображается красная иконка облака со стрелкой (Heroicons) в ячейке "Барьер".
   - **Единый вид информационных карточек:**
     - `openSokbEdit` и `openCorrectiveEdit`: блок информации о проверке теперь использует стили `.violation-card` (как в лобби), включая строку `checkId`, метод/барьер, организацию/объект.
     - Стили `.corr-viol-info` упрощены (`background: transparent`).
   - **Карточка проверки — placeholder объекта:**
     - `resetCard` и `openCardForEdit`: значение по умолчанию для объекта изменено с `—` на `Ввести` (логичнее для текстового ввода).
   - **Оверлей описания нарушения:**
     - `rows="7"` для textarea, placeholder убран (соответствует паттерну оверлеев с пустым полем).
   - `APP_BUILD` → `pkb-v346`, кэш SW → `pkb-static/api-v346`.
2. **pkb-v345 — единообразные кнопки "Назад" (как в screen-export):**
   - **screen-find (Проверки):**
     - Добавлен специфичный CSS `#screen-find .find-btn-cancel` — стиль полностью совпадает с `.screen-back-btn` (width: calc((100vw - 48px) / 3), height: 53px, background: var(--gray-btn), font-size: 15px, font-weight: 700, border-radius: var(--btn-radius)).
     - Кнопка остаётся в `.find-bottom` между счётчиками, но имеет фиксированную ширину и точный внешний вид эталонной кнопки.
   - **screen-meropr-menu (Мероприятия):**
     - HTML: кнопка "Назад" вынесена из `.proverki-menu-content` в отдельный `.settings-footer` внизу экрана.
     - Класс кнопки изменён на `.screen-back-btn` — полное совпадение с эталоном.
   - **screen-card-photo (Добавьте фотографии нарушений):**
     - HTML: `.card-bottom` заменён на `.settings-footer`, класс кнопки изменён на `.screen-back-btn`.
     - `.photo-scroll` уже имеет `flex: 1`, поэтому footer корректно прижимается к низу экрана.
   - `APP_BUILD` → `pkb-v345`, кэш SW → `pkb-static/api-v345`.
2. **pkb-v344 — placeholder и цвет текста в ячейках окна внесения проверки:**
   - **"Барьер работоспособен":**
     - HTML placeholder изменён с "Выбрать" на "—" (по умолчанию).
     - `setWorksCellDisplay` разделена логика: `!value` → "—", `'Выбрать'` → "Выбрать" (placeholder).
     - После выбора барьера без чек-листа `syncWorksCellState` вызывает `setWorksCellDisplay('Выбрать')` — кнопка активна для выбора.
   - **"Проверяемая организация" и "Проверяемый объект" — черный текст после ввода:**
     - `openDrumOrg`: теперь удаляет класс `.placeholder` у `val-org` после выбора организации.
     - `openTextOverlay` (`currentTextCallback`): теперь удаляет класс `.placeholder` при непустом значении, добавляет при пустом (исправлено для `val-obj` и других текстовых полей).
   - **"Описание нарушений" — черный текст после ручного ввода (без чек-листа):**
     - `applyDesc`: теперь удаляет класс `.placeholder` при непустом описании, добавляет при пустом.
   - `APP_BUILD` → `pkb-v344`, кэш SW → `pkb-static/api-v344`.
2. **pkb-v343 — кнопка "Добавить фото" и screen фотографий нарушений:**
   - **HTML:** добавлена кнопка `btn-card-photo` под ячейкой "Проверяемый барьер" (рядом с `btn-card-checklist`).
   - **CSS:** добавлены стили `.photo-scroll`, `.photo-body`, `.photo-violation-item`, `.photo-violation-text`, `.photo-btn-row`, `.photo-action-btn` (оранжевая кнопка с иконкой SVG), `.card-btn-gray` (серая капсульная кнопка "Назад").
   - **JS логика кнопки (`updateCardPhotoButton`):**
     - Барьер не выбран — кнопка скрыта.
     - Барьер имеет чек-лист, но он не заполнен — кнопка видима, но тусклая (`.disabled`, `opacity: 0.45`, `pointer-events: none`).
     - Барьер имеет чек-лист и он заполнен — кнопка активна.
     - Барьер не имеет чек-листа — кнопка активна (вместо "Заполнить чек-лист").
   - **Новый screen `screen-card-photo`:**
     - **Вариант 1 (без чек-листа):** заголовок "Добавьте фотографии нарушений", по центру кнопки "Камера" и "Прикрепить" с SVG-иконками (camera, paper-clip из Heroicons), серая капсульная кнопка "Назад".
     - **Вариант 2 (с чек-листом):** заголовок + список текстов нарушений из чек-листа. Под каждым нарушением — кнопки "Камера" и "Прикрепить". Серая кнопка "Назад" внизу.
     - Функции `openCardPhotoScreen()`, `closeCardPhotoScreen()`, `renderCardPhotoScreen()`, `renderCardPhotoSimple()`, `cardPhotoCamera()`, `cardPhotoAttach()`.
   - `screen-card-photo` добавлен в `PULL_OFF`.
   - `updateCardPhotoButton()` вызывается при открытии карточки, смене барьера, применении/отмене чек-листа.
   - `APP_BUILD` → `pkb-v343`, кэш SW → `pkb-static/api-v343`.
2. **pkb-v342 — синий контур полей ввода в оверлеях СОКБ/корректирующих мероприятий:**
   - В CSS добавлены специфичные правила для `#overlay-corr-desc .overlay-textarea` и `#overlay-corr-text .overlay-input`:
     - `border-color: var(--navy)` (вместо `var(--orange)`)
     - При фокусе тоже `border-color: var(--navy)`
   - `APP_BUILD` → `pkb-v342`, кэш SW → `pkb-static/api-v342`.
2. **pkb-v341 — UI-правки в окнах внесения оспаривания в СОКБ и корректирующих мероприятий:**
   - **П.1 — Видимые границы ячеек:**
     - Добавлен `-webkit-appearance: none; appearance: none;` в CSS `.corr-m-desc` и `.corr-m-input` — устраняет iOS-специфичное скрытие границ.
     - Inline textarea/input в `createSokbAttemptEl` и `createCorrMeasureEl` заменены на div'ы с классом `.corr-m-cell`, у которых граница `var(--navy)` видна всегда.
   - **П.2 — Заголовок мероприятия:**
     - В `createCorrMeasureEl` заголовок изменён с `Корректирующее мероприятие №N` на `Описание мероприятия №N`.
   - **П.3 — Оверлеи для редактирования ячеек:**
     - **СОКБ:** ячейка "Обоснование оспаривания" теперь открывает `overlay-corr-desc` с заголовком `Введите обоснование для оспаривания` (синий тон оверлея `#eaf0fb`, белое поле textarea).
     - **Корректирующие мероприятия:** ячейка "Описание мероприятия" открывает `overlay-corr-desc` с заголовком `Опишите мероприятие`.
     - **Корректирующие мероприятия:** ячейка "Ответственный департамент организация" открывает `overlay-corr-text` с заголовком `Введите ответственный департамент организацию` (синий тон оверлея, белое поле input).
     - Применение значений через оверлей обновляет DOM и состояние модели без полной перерисовки списка.
   - `APP_BUILD` → `pkb-v341`, кэш SW → `pkb-static/api-v341`.
2. **pkb-v336 — периодическая проверка push-подписки (раз в неделю):**
   - Добавлены константы `PUSH_SUB_CHECK_KEY` и `PUSH_SUB_CHECK_INTERVAL_MS` (7 дней) в `index.html`
   - Добавлена функция `checkPushSubscriptionWeekly(reg)`:
     - Проверяет время последней проверки в `localStorage`
     - Если прошла неделя — получает текущую подписку через `pushManager.getSubscription()`
     - Если подписки нет — вызывает `subscribeToPush(reg)` для пересоздания
     - Если подписка есть — переотправляет её на сервер через `sendSubscriptionToServer()`
     - Обновляет timestamp последней проверки
   - Функция вызывается:
     - После регистрации Service Worker (`registerSW()`)
     - При возврате приложения на передний план (`visibilitychange`)
   - `APP_BUILD` → `pkb-v336`, кэш SW → `pkb-static/api-v336`.
2. **pkb-v335 — нежирный красный "Просрочено":**
   - Убран `font-weight: 600` из CSS `.find-table td.corr-status-overdue` — теперь "Просрочено" в списке корректирующих мероприятий отображается красным, но не жирным.
   - `APP_BUILD` → `pkb-v335`, кэш SW → `pkb-static/api-v335`.
3. **pkb-v334 — нежирный красный "Нет":**
   - Убран `font-weight: 700` из CSS `.field-val-works-no` и `.detail-val-works-no`, а также из inline-стиля в JS (`setWorksCellDisplay`).
   - `APP_BUILD` → `pkb-v334`.
4. **pkb-v333 — push-уведомления + оверлей обновления + UI:**
   - **Обработка клика по push-уведомлению (`sw.js`):**
     - При клике SW отправляет `NOTIFICATION_CLICK` всем клиентам (вкладкам/PWA)
     - Если приложение открыто — получает сообщение, чистит API-кэш, загружает свежие данные и переходит на лобби
     - Если закрыто — открывается URL с `?nc=1`
   - **Обработка `?nc=1` в `index.html`:**
     - Удаляется сохранённый экран из `sessionStorage`
     - Переход на лобби (`goLobby()`)
     - Обновление списков (`refreshActiveRecordLists()`)
     - Параметр `nc=1` убирается из URL через `history.replaceState()`
   - **Убран `requireInteraction` из push-уведомлений** — теперь уведомления можно смахнуть, не требуют обязательного взаимодействия
   - **Исправлено повторное появление оверлея "Обновить приложение":**
     - Добавлен sessionStorage флаг `pkb-update-done` после применения обновления
     - `evaluateAppUpdate()` проверяет флаг и пропускает показ оверлея в той же сессии
   - `APP_VERSION` → `1.07`, `APP_BUILD` → `pkb-v333`, кэш SW → `pkb-static/api-v333`
5. **pkb-v332 — безопасные правки по аудиту (без изменения контрактов API):**
   - **Защита статики (`server.js`):** добавлен deny-list перед `express.static(ROOT)` — блокируются `*.env*`, `*.db/-wal/-shm`, `scripts/`, `docs/`, `node_modules/`, `data/backups|snapshots/` (HTTP 403). Публичные ассеты (иконки, манифест, паспорта, снипшоты) продолжают раздаваться.
   - **Удалены дефолтные PIN (1111/3333/2222):**
     - `lib/settings-store.js`: `EMPTY_SETTINGS.passwords` → `{admin:null, inspector:null, orgs:{}}`, `usedPasswords` → `[]`.
     - `index.html`: `getDefaultPasswords()` → `{admin:null, inspector:null, orgs:{}}`, `getDefaultUsedPasswords()` → `[2003]`, `ensureOrgPasswords`/`renderPasswordsScreen` больше не подставляют `2222`.
     - Из дефолтов остался только суперпользователь `2003`; вся остальная авторизация — из сохранённых паролей в «Управление паролями».
     - Защитые `isDefaultAdminPin`/`isDefaultInspectorPin` оставлены как «запрет слабых пин-кодов» при записи.
   - **`PUSH_MIN_RECORDS` (`lib/github-persist.js`):** порог минимального числа записей для пуша на GitHub настраивается через env (дефолт `50`), поведение по умолчанию не изменилось.
   - `APP_VERSION` → `1.06`, `APP_BUILD` → `pkb-v332`, кэш SW → `pkb-static/api-v332`.
6. **Предыдущие изменения (pkb-v331):** fix overdue-date highlighting for completed corrective measures.

## Что НЕ сделано / следующие шаги
- [x] Закоммитить pkb-v341–pkb-v356 в master.
- [ ] Закоммитить изменения pkb-v357–pkb-v361 в master.
- [ ] Деплой на Timeweb.
- [ ] (Отложено по решению) Серверная авторизация на мутирующие API, перенос суперпользователя на сервер, CORS-`*`, rate-limiting — меняют контракт клиента, риск поломок.

## Полезные ссылки
- Production: https://proverkikb.tw1.ru/
- База: SQLite + GitHub (`proverki-kb-data`)
- Деплой: ручной через Timeweb App Platform
- Локальный запуск: `npm start` → http://localhost:3000

## Архитектура (кратко)
- **Frontend:** `index.html` (SPA, vanilla JS), `sw.js` (PWA кэш)
- **Backend:** Node.js + Express, API в `api/`
- **База:** SQLite (`lib/db.js`)
- **Синхронизация:** GitHub (`lib/github-persist.js`)
- **Push-уведомления:** web-push, VAPID ключи в `index.html` + `.env`
